<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FureverHome"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.shelter_management.AnimalListings" />
        <activity android:name=".ui.shelter_management.AnimalDetailActivity" />
        <activity android:name=".ui.shelter_management.AnimalUpdateActivity" />
        <activity android:name=".ui.shelter_management.AnimalAddActivity" />
        <activity android:name=".ui.tasks.TaskDetailsActivity" />
        <activity android:name=".ui.events.EventGenerateActivity" />
        <activity android:name=".ui.events.EventDetailsActivity"/>
        <activity android:name=".ui.community.DiscussionGenerateActivity" />
        <activity android:name=".ui.community.DiscussionDetailsActivity"/>
    </application>

</manifest>