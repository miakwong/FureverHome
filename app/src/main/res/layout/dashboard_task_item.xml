<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Task Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Task Icon -->
            <ImageView
                android:id="@+id/taskIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_task"
                app:tint="@color/md_theme_light_onSurface" />

            <!-- Task Title -->
            <TextView
                android:id="@+id/taskTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Task Title"
                android:textStyle="bold"
                android:textSize="16sp" />

            <!-- Arrow Icon to Expand/Collapse -->
            <ImageView
                android:id="@+id/toggleArrow"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_arrow_down"
                app:tint="@color/md_theme_light_onSurface"
                android:layout_gravity="end"/>

            <!-- Add a spacer View to push elements to the right -->
            <View
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <!-- Arrow Icon to go to task details -->
            <ImageView
                android:id="@+id/arrowRight"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_arrow_right"
                app:tint="@color/md_theme_light_onSurface"
                android:contentDescription="Go to task details"
                android:layout_gravity="end"/>
        </LinearLayout>

        <!-- Task Details (Initially Hidden) -->
        <LinearLayout
            android:id="@+id/taskDetailsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <!-- Task Status -->
            <TextView
                android:id="@+id/taskStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Status"
                android:textSize="14sp"
                android:textColor="@color/md_theme_light_onSecondary" />
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
